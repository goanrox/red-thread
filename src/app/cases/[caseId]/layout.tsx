// Phase 1 — Case shell layout (persistent nav bar)
// Full implementation in Phase 1 build.

export default function CaseLayout({ children }: { children: React.ReactNode }) {
  return <div className="min-h-screen">{children}</div>;
}
